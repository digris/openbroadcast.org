machine:
  services:
    - redis
  post:
    - pyenv global 2.7

dependencies:
  post:
    - pip install tox==2.6.0
    - pip install numpy==1.8.0

test:
  override:
    - tox

general:
  branches:
    ignore:
      - translation
